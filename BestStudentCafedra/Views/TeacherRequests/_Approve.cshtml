@model BestStudentCafedra.Models.TeacherRequest

<div class="modal-content border-success">
    <div class="modal-header">
        <div>
            <h4>Принятие запроса</h4>
        </div>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
    </div>
    <div class="modal-body">
        <div class="text-center">
            В случае принятия этого запроса вы будете назначены для указанной работы в качестве: 
            <div class="text-info"><b>@Html.DisplayFor(x => x.RequestType)</b></div>
        </div>
        <form asp-action="Approve">
            <div class="form-row mb-3">
                <input required type="hidden" asp-for="Id" />
                <input hidden name="returnUrl" value="@ViewBag.returnUrl" />
            </div>
            <div class="form-group w-100 mb-0">
                <input type="submit" value="Принять" class="btn btn-success w-100" />
            </div>
        </form>
    </div>
</div>