@model BestStudentCafedra.Models.Activity

@{
    ViewData["Title"] = "Подробности о работе";
}

<h1>Подробности о работе</h1>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Number)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Number)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Title)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Title)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.MaxPoints)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.MaxPoints)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Type)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Type.Name)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Id">Изменить</a> |
    <a asp-action="Index">Вернуться в список работ</a>
</div>

<br/>
<h4>Оценки:</h4>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ActivityProtections.FirstOrDefault().Student)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ActivityProtections.FirstOrDefault().Points)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ActivityProtections.FirstOrDefault().ProtectionDate)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.ActivityProtections)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Student.FullName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Points)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProtectionDate)
            </td>
        </tr>
        }
    </tbody>
</table>
